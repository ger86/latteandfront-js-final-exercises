const plus = (x, y) => x + y;
const minus = (x, y) => x - y;
const multiply = (x, y) => x * y;
const divide = (x, y) => x / y;

const calculator = {
  plus,
  minus,
  multiply,
  divide
};

export default calculator;